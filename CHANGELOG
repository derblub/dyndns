Please view this file on the master branch, on stable branches it's out of date.


v 0.2
  - Fix: prevent continuing script when we don't have $MY_IP
  - Change: mysql `--batch` and `--skip-column-names` for `SELECT`'s

v 0.1
  - released
